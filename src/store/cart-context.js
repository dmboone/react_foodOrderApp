import React from 'react';

// the purpose if this file is really just to have better autocomplete
React.createContext({
    items: [],
    totalAmount: 0,
    addItem: (item) => {

    },
    removeItem: (id) => {

    },
});

export default CartContext;